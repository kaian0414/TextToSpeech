<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp"
    tools:context=".MainActivity">
    
    <!-- EditText which will input text to speech -->
    <EditText
        android:id="@+id/hear_inputbox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="100dp"
        android:gravity="start"
        android:padding="5dp"
        android:hint="Enter Text to Speech..."/>

    <!-- Button to onclick reading the text -->
    <Button
        android:id="@+id/hear_speakbtn"
        android:text="Speak"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/hear_inputbox"
        android:layout_alignParentStart="true"
        android:drawableLeft="@drawable/ic_speak"
        android:drawablePadding="5dp"/>

    <!-- Button to onclick stop reading the text -->
    <Button
        android:id="@+id/hear_stopbtn"
        android:text="Stop"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/hear_inputbox"
        android:layout_alignParentEnd="true"
        android:drawableLeft="@drawable/ic_speakstop"
        android:drawablePadding="5dp"/>

</RelativeLayout>